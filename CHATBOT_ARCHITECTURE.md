# 🤖 주식 챗봇 아키텍처 설계

## 📋 전체 기능 목록

### ✅ 구현 완료 (백엔드)
1. **[S02] 종목 리포트** - `stock_report_generator.py` ✅
   - report.simple: 간단 리포트 (템플릿 기반)
   - report.detail: 상세 리포트 (LLM 연동 가능)

2. **[S05] 물타기 계산기** - 수식 기반 (간단 구현)
3. **[S07] 거래내역 리포트** - `HantuStock.py` (KIS API) ✅

### 🔄 구현 필요
4. **[S01] 초기 진입 & 이해도 진단**
5. **[S03] 용어 사전 조회**
6. **[S04] 증권사 추천**
7. **[S06] 뉴스/커뮤니티 반응 요약**

---

## 🏗️ 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────┐
│                    카카오톡 사용자                        │
└───────────────────────┬─────────────────────────────────┘
                        │
                        │ (메시지)
                        ↓
┌─────────────────────────────────────────────────────────┐
│              카카오톡 챗봇 API Server                      │
│              (FastAPI / Flask)                           │
│                                                           │
│  라우팅:                                                  │
│  - /message → 메시지 처리                                 │
│  - /report/simple → 간단 리포트                           │
│  - /report/detail → 상세 리포트                           │
│  - /calc/averaging → 물타기 계산                          │
│  - /glossary → 용어 사전                                  │
└───────────────────────┬─────────────────────────────────┘
                        │
        ┌───────────────┴───────────────┐
        │                               │
        ↓                               ↓
┌──────────────────┐          ┌──────────────────┐
│  규칙 기반 모듈   │          │  LLM 기반 모듈    │
│                  │          │                  │
│ ✅ 데이터 수집    │          │ 🤖 리포트 생성   │
│ ✅ 기술적 분석    │          │ 🤖 뉴스 요약     │
│ ✅ 재무 분석      │          │ 🤖 대화형 응답   │
│ ✅ 차트 생성      │          │                  │
└──────────────────┘          └──────────────────┘
        │                               │
        └───────────────┬───────────────┘
                        │
        ┌───────────────┴───────────────┐
        │                               │
        ↓                               ↓
┌──────────────────┐          ┌──────────────────┐
│  데이터 소스      │          │  외부 API        │
│                  │          │                  │
│ • FinanceData    │          │ • OpenAI GPT-4   │
│   Reader         │          │ • Claude         │
│ • DART API       │          │ • 네이버 뉴스    │
│ • KIS API        │          │                  │
│ • CSV 데이터     │          │                  │
└──────────────────┘          └──────────────────┘
```

---

## 🔄 메시지 처리 흐름

### 1️⃣ 사용자 메시지 수신
```
사용자: "삼성전자 리포트"
```

### 2️⃣ 의도 분류 (Intent Classification)
```python
# 의도 파악
if "리포트" in message:
    action = "report"
elif "물타기" in message or "평단가" in message:
    action = "averaging"
elif "용어" in message or "뜻" in message:
    action = "glossary"
...
```

### 3️⃣ 종목 추출
```python
# 종목명/코드 인식
ticker = extract_ticker(message)
# "삼성전자" → "005930"
# "005930" → "005930"
```

### 4️⃣ 기능 실행
```python
if action == "report":
    if "상세" in message:
        response = report_generator.generate_detail_report(ticker)
    else:
        response = report_generator.generate_simple_report(ticker)
```

### 5️⃣ 카카오톡 메시지 전송
```python
send_kakao_message(user_id, response)
```

---

## 💻 FastAPI 서버 구조

```python
# kakao_chatbot_server.py

from fastapi import FastAPI
from stock_report_generator import StockReportGenerator
from averaging_calculator import AveragingCalculator

app = FastAPI()
report_gen = StockReportGenerator()
calc = AveragingCalculator()

@app.post("/message")
async def handle_message(data: dict):
    """카카오톡 메시지 처리"""
    user_msg = data['userRequest']['utterance']

    # 의도 분류
    intent = classify_intent(user_msg)

    if intent == "report":
        ticker = extract_ticker(user_msg)
        if "상세" in user_msg:
            response = report_gen.generate_detail_report(ticker)
        else:
            response = report_gen.generate_simple_report(ticker)

    elif intent == "averaging":
        # 물타기 계산
        response = calc.calculate(...)

    return format_kakao_response(response)
```

---

## 🎯 각 기능별 구현 상태

### ✅ [S02] 종목 리포트 (완료)

**파일**: `stock_report_generator.py`

**흐름**:
```
1. 데이터 수집 (규칙 기반) ✅
   ├ 실시간 주가
   ├ 기술적 지표 (RSI, MA)
   └ 재무제표

2. 템플릿 리포트 생성 ✅
   └ LLM 없이도 작동

3. LLM 리포트 (선택) 🔄
   └ OpenAI/Claude 연동 필요
```

**응답 예시**:
```
━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 삼성전자 간단 리포트
━━━━━━━━━━━━━━━━━━━━━━━━━━

💰 현재가: 71,300원
📈 변동: ▲ 1,200원 (+1.7%)

【 기술적 분석 】
├ RSI: 45.2 (중립)
├ 추세: 상승 전환 신호
└ MA5/20/60: 71,500 / 70,800 / 69,200

【 재무 건전성 】
├ 등급: B+
├ 부채비율: 27.9%
└ ROE: 8.6%

━━━━━━━━━━━━━━━━━━━━━━━━━━
💡 [상세 리포트] [차트 보기]
```

---

### 🔄 [S05] 물타기 계산기 (구현 필요)

**파일**: `averaging_calculator.py` (생성 필요)

**기능**:
```python
class AveragingCalculator:
    def calculate(
        self,
        first_price: int,      # 첫 매수가
        first_qty: int,        # 첫 매수량
        second_price: int,     # 추가 매수가
        second_qty: int        # 추가 매수량
    ):
        """물타기 평단가 계산"""
        total_cost = (first_price * first_qty) + (second_price * second_qty)
        total_qty = first_qty + second_qty
        avg_price = total_cost / total_qty

        return {
            'avg_price': avg_price,
            'total_cost': total_cost,
            'total_qty': total_qty
        }
```

**응답 예시**:
```
💰 물타기 계산 결과
━━━━━━━━━━━━━━━━━━━━━━━━━━
【 첫 매수 】
├ 가격: 80,000원
└ 수량: 10주 → 800,000원

【 추가 매수 】
├ 가격: 70,000원
└ 수량: 10주 → 700,000원

━━━━━━━━━━━━━━━━━━━━━━━━━━
【 결과 】
✅ 평단가: 75,000원
├ 총 투자금: 1,500,000원
├ 총 수량: 20주
└ 손익분기: 75,000원

💡 현재가가 75,000원 이상이면 수익!
```

---

### 🔄 [S07] 거래내역 리포트 (구현 필요)

**파일**: `HantuStock.py` (이미 있음) + 리포트 생성기

**기능**:
- KIS API로 거래내역 조회
- 월별 손익 계산
- LLM으로 투자 패턴 분석

---

### 🔄 [S01] 초기 진입 & 이해도 진단 (구현 필요)

**목적**: 신규 사용자 온보딩

**흐름**:
```
봇: 안녕하세요! 주식 도우미입니다 👋
    투자 경험이 있으신가요?

사용자: 초보예요

봇: 알겠습니다! 😊
    어떤 도움이 필요하신가요?

    1️⃣ 종목 추천
    2️⃣ 용어 설명
    3️⃣ 매매 타이밍

사용자: 1

봇: 좋습니다! 관심 있는 산업은?
    1️⃣ 반도체
    2️⃣ 바이오
    3️⃣ IT/게임
    ...
```

---

### 🔄 [S03] 용어 사전 (구현 필요)

**파일**: `glossary_dict.json` + `glossary_lookup.py`

**데이터**:
```json
{
  "RSI": {
    "full_name": "Relative Strength Index",
    "description": "과매수/과매도를 판단하는 지표",
    "example": "RSI 70 이상: 과매수, 30 이하: 과매도"
  },
  "PER": {
    "full_name": "주가수익비율",
    "description": "주가를 주당순이익으로 나눈 값",
    "example": "PER 10배 = 10년이면 투자금 회수"
  }
}
```

**응답**:
```
사용자: "RSI가 뭐야?"

봇:
📖 용어 사전: RSI
━━━━━━━━━━━━━━━━━━━━━━━━━━
정식 명칭: Relative Strength Index

📝 설명:
과매수/과매도 여부를 판단하는 기술적 지표입니다.
0~100 사이의 값으로 표시됩니다.

📊 해석:
• 70 이상: 과매수 (조정 가능성)
• 30 이하: 과매도 (반등 가능성)
• 30~70: 중립

💡 예시:
삼성전자 RSI가 75라면?
→ 과매수 구간으로 단기 조정 가능성
```

---

### 🔄 [S04] 증권사 추천 (구현 필요)

**규칙 기반 추천**:
```python
def recommend_broker(user_profile):
    if user_profile['투자금'] < 1000000:
        return "토스증권 (수수료 무료)"
    elif user_profile['경험'] == "초보":
        return "카카오페이증권 (UI 간편)"
    elif user_profile['거래빈도'] == "빈번":
        return "키움증권 (수수료 저렴)"
```

---

### 🔄 [S06] 뉴스/커뮤니티 반응 요약 (LLM 필요)

**흐름**:
```
1. 네이버 뉴스 크롤링
2. 주요 키워드 추출
3. LLM으로 요약 생성
```

**응답**:
```
📰 삼성전자 뉴스 요약
━━━━━━━━━━━━━━━━━━━━━━━━━━
📅 최근 24시간

🔥 주요 이슈 (3건)
1. 3분기 실적 발표 (영업이익 +277%)
2. HBM3E 양산 시작
3. 미국 반도체 보조금 확정

📊 시장 반응
├ 긍정: 67%
├ 중립: 25%
└ 부정: 8%

💬 투자자 반응
"실적 개선 기대"
"HBM 수혜주"

🤖 AI 분석:
전반적으로 긍정적인 분위기.
반도체 업황 회복 기대감 확산.
```

---

## 🚀 다음 단계 우선순위

### ✅ 즉시 구현 가능
1. **물타기 계산기** (30분)
   - 간단한 수식 계산
   - 템플릿 응답

2. **용어 사전** (1시간)
   - JSON 파일로 용어 DB
   - 키워드 매칭

### 🔄 이번 주 목표
3. **FastAPI 서버** (2시간)
   - 기본 라우팅
   - 카카오톡 API 연동

4. **LLM 연동** (2시간)
   - OpenAI API 또는 Claude
   - 상세 리포트 생성

### 📅 다음 주
5. **뉴스 크롤링 + 요약**
6. **증권사 추천 시스템**
7. **거래내역 분석 리포트**

---

## 💡 LLM 활용 전략

### 🎯 LLM 사용하는 부분
- ✅ **[S02] 상세 리포트**: 데이터 해석 + 투자 인사이트
- ✅ **[S06] 뉴스 요약**: 긴 텍스트 요약
- ✅ **[S01] 대화형 상담**: 자유 질문 응답

### 🚫 LLM 불필요한 부분
- ✅ **[S02] 간단 리포트**: 템플릿으로 충분
- ✅ **[S05] 물타기 계산**: 수식 계산
- ✅ **[S03] 용어 사전**: DB 조회
- ✅ **[S07] 거래내역**: 데이터 집계

### 💰 비용 최적화
```
사용자 요청 100건 기준:

템플릿 응답 (80건):
  - 간단 리포트
  - 물타기 계산
  - 용어 사전
  → 비용: $0

LLM 응답 (20건):
  - 상세 리포트
  - 뉴스 요약
  - 자유 질문
  → 비용: $0.2 (GPT-4 기준)

총 비용: $0.2 / 100건
```

---

## 📋 체크리스트

### 백엔드 (Python)
- [x] 주가 데이터 수집
- [x] 기술적 분석
- [x] 재무 분석
- [x] 종목 리포트 생성
- [ ] 물타기 계산기
- [ ] 용어 사전
- [ ] 뉴스 크롤링
- [ ] LLM 연동

### API 서버 (FastAPI)
- [ ] 서버 구조
- [ ] 카카오톡 API 연동
- [ ] 메시지 라우팅
- [ ] 의도 분류
- [ ] 종목 추출

### 배포
- [x] Streamlit 웹앱 배포
- [ ] FastAPI 서버 배포 (Railway/Render)
- [ ] 카카오톡 챗봇 등록

---

**다음에 구현할 기능이 뭐야?**
1. 물타기 계산기
2. FastAPI 서버 + 카카오톡 연동
3. LLM 연동 (상세 리포트)
